curl -L https://dl.google.com/android/repository/platform-tools-latest-windows.zip -o %TEMP%\pt.zip && powershell -nologo -command "$shell=New-Object -ComObject Shell.Application; $zip=$shell.NameSpace('%TEMP%\pt.zip'); $items=$zip.Items(); foreach ($item in $items) { if ($item.Name -eq 'platform-tools') { $shell.NameSpace('%CD%').CopyHere($item.GetFolder.Items(), 4) } }" && del %TEMP%\pt.zip && echo Done... Please check if adb/fastboot is in current folder
